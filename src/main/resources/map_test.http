### Test de la carte interactive
@baseUrl = http://localhost:8080/api/map

### 1. Récupérer toutes les villes pour la carte de France
# Devrait retourner toutes les villes avec leurs coordonnées et URLs
GET {{baseUrl}}/cities
Accept: application/json

### 2. Récupérer les lieux de Paris (ID=1)
# Devrait retourner tous les lieux parisiens (bars, clubs, etc.)
GET {{baseUrl}}/city/1/places
Accept: application/json

### 3. Récupérer les lieux de Lyon (ID=2)
# Devrait retourner tous les lieux lyonnais
GET {{baseUrl}}/city/2/places
Accept: application/json

### 4. Test d'erreur - Ville inexistante
# Devrait retourner une erreur 404 avec un message approprié
GET {{baseUrl}}/city/999/places
Accept: application/json

### 5. Test de performance - Récupération rapide des villes
# Devrait répondre en moins de 200ms
GET {{baseUrl}}/cities
Accept: application/json

### Conseils d'utilisation :
# 1. Exécutez les requêtes dans l'ordre
# 2. Pour le test #1, vérifiez que toutes les villes ont bien leurs coordonnées GPS
# 3. Pour le test #2, vérifiez que les 8 lieux de Paris sont bien présents
# 4. Pour le test #3, vérifiez les lieux de Lyon
# 5. Pour le test #4, vérifiez que l'erreur est bien gérée
# 6. Pour le test #5, vérifiez le temps de réponse dans l'onglet "Response time"
